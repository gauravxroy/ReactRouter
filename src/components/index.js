// this file can be used to import all the components at one place
//and then we can use it directly in another component whithout importing it from different files and folders
import Home from "./Home/Home";
import Header from "./Header/Header";
import Layout from "./Layout";
import Footer from "./Footer/Footer";
import About from "./About/About";
import NotFound from "./Error/NotFound";
import User from "./User/User";
export { Header, Home, Layout, About, Footer, NotFound, User };
